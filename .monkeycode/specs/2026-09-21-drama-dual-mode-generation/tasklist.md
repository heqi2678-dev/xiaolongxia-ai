# 需求实施计划

- [x] 1. 新增段划分与段计划纯函数模块 `src/drama/takes.js`
  - [x] 1.1 实现常量与 `limits(project)`，返回 `{ min, target, max }`，`target` 取 `project.takeTarget` 缺失按 15 并夹到 `[4, 30]`（需求 2.3，设计「段划分与段计划」）
  - [x] 1.2 实现 `group(project, opts)`，按分镜顺序贪心累加，命中目标断段、超硬上限必拆、末段小于下限并入前段、单镜超硬上限返回不可分段标记（需求 2.1、2.2、2.4、2.5、2.6）
  - [x] 1.3 实现 `plan(project, shotIds)`，返回 `[{ sid, seq, start, end }]`，首镜 `start` 为 0、末镜 `end` 等于段时长（需求 6.1，设计正确性属性 4）
  - [x] 1.4 实现 `sync(project)`，按 `shotIds` 签名保留既有段素材与状态、重算段时长与段计划、刷新分镜到段的索引（需求 2.7、8.4）
  - [x] 1.5 实现 `takeOf(project, sid)`、`segmentOf(project, sid)`、`markDirty(project, sid)`（需求 6.1、3.7）
  - [ ]* 1.6 为 `group` 编写单元测试，覆盖总时长小于下限、恰好命中目标、候选超硬上限必拆、末段小于下限并入前段、单镜超硬上限（需求 2.4、2.5、2.6）
  - [ ]* 1.7 为 `plan` 编写单元测试，覆盖空段、单镜、零时长与相邻镜首尾相接（设计正确性属性 4）
  - [ ]* 1.8 为 `sync` 编写单元测试，覆盖幂等与签名命中时段素材保留（设计正确性属性 8）
  - [ ]* 1.9 编写属性测试，验证段集合为连续不重叠划分且每段时长等于段内分镜时长之和（设计正确性属性 1、2、3、5）

- [x] 2. 扩展工程模型与迁移 `src/drama/project.js`
  - [x] 2.1 在 `blank` 中补齐 `shotMode`、`takeTarget`、`takes` 默认值（需求 1.3、设计「数据模型」）
  - [x] 2.2 扩展 `migrate`：补齐 `shotMode`、`takeTarget`、`takes`，并调用 `D.takes.sync` 重建段集合（需求 8.1、8.4）
  - [x] 2.3 让 `persistAssets` 与 `hydrateAssets` 覆盖 `takes[].videoUrl`（需求 8.2、8.3、设计「数据模型」）
  - [ ]* 2.4 为 `migrate` 编写单元测试，覆盖旧工程字段补齐、幂等与 `takes` 重建（需求 8.1、设计正确性属性 8）
  - [ ]* 2.5 为段素材持久化与还原编写单元测试（需求 8.2、8.3）

- [x] 3. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 4. 扩展视频适配器 `src/drama/adapters/video.js`
  - [x] 4.1 扩展 `taskCreate` 参考素材类型，支持 `reference_video` 与 `reference_audio`，并保留既有 `reference_image` 与首帧去重（需求 3.3，设计「视频适配器」）
  - [x] 4.2 新增 `video.edit(opts, onProgress, signal)` 入口，`content` 至少含一个参考视频，文本参数固定 `--ratio adaptive --duration -1`（需求 4.2，设计依赖约束）
  - [x] 4.3 扩展 `seedanceText` 支持 `adaptive` 与 `audio === false` 参数（需求 3.4，设计依赖约束）
  - [x] 4.4 实现 `assertTaskParams(opts)`，在提交前校验参考视频时长、视频编辑与首帧任务的参数合法性并抛 `BAD_PARAM`（设计「错误处理」）
  - [x] 4.5 收敛分辨率档位到 `480p` 与 `720p`，整段模式默认 `720p`（设计依赖约束）
  - [ ]* 4.6 为参数拼装与 `assertTaskParams` 校验矩阵编写单元测试（设计依赖约束）
  - [ ]* 4.7 为 `edit` 任务体与轮询编写单元测试，覆盖 `ratio` 为 `adaptive` 与 `duration` 为 `-1`（需求 4.2）

- [x] 5. 扩展生成编排 `src/drama/engine.js`
  - [x] 5.1 实现 `generateTake(project, takeId, opts)`，含段时长校验、段级并发抑制、段素材落盘、段与段内分镜状态联动（需求 3.1、3.5、3.6、3.7，设计「生成编排」）
  - [x] 5.2 实现 `editTake(project, takeId, sid, opts)`，按分镜段内区间组装编辑意图、以段素材为参考视频、成功后替换段素材且段计划不变（需求 4.1、4.2、4.3、4.5）
  - [x] 5.3 实现 `abortTake(takeId)` 并让 `abortAll` 清理段级任务键，段级与镜级键分属不同命名空间（设计「状态机与联动」）
  - [x] 5.4 在 `generateShot` 入口增加 `shotMode === "take"` 的守卫，抛 `MODE_SHOT_ONLY`（需求 1.5、5.1）
  - [x] 5.5 实现段模式未配置回退，产出占位段素材并标注 `fallback`（需求 3.8，设计「错误处理」）
  - [ ]* 5.6 为 `generateTake` 编写单元测试，覆盖成功、失败保留原段素材与并发抑制（需求 3.5、3.6、3.7）
  - [ ]* 5.7 为 `editTake` 编写单元测试，覆盖替换段素材且段时长与段计划不变（需求 4.3，设计正确性属性 6）
  - [ ]* 5.8 编写属性测试，验证切换生成模式不改动任何素材字段（需求 1.4，设计正确性属性 7）

- [x] 6. 实现提示词与参考素材组装 `src/drama/takes.js`
  - [x] 6.1 实现 `prompt(project, take)`，按一致性约束、角色设定、时间段描述、锁定项四段组织，时间段取段计划（需求 3.2，设计「提示词与参考素材」）
  - [x] 6.2 在时间段描述中写入段内台词，供有声视频同次产出对白与口型（需求 3.4、6.4）
  - [x] 6.3 实现 `refGroups(project, take)`，合并段内出场角色并复用每角色限流与轮询摊平规则（需求 3.2、3.3）
  - [ ]* 6.4 为 `prompt` 编写单元测试，覆盖时间段边界与台词拼装（需求 3.2、3.4）
  - [ ]* 6.5 为 `refGroups` 编写单元测试，覆盖多角色同框与每角色上限（需求 3.3）

- [x] 7. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 8. 视图层：生成模式开关与段操作 `src/drama/manual.js`、`src/drama/auto.js`
  - [x] 8.1 增加生成模式开关并持久化 `shotMode`，切换时保留已生成的分镜素材与段素材（需求 1.1、1.2、1.4）
  - [x] 8.2 增加段视图，展示镜头段范围与段内分镜，并在时间轴叠加段边界标记（需求 2.9、6.2）
  - [x] 8.3 增加整段生成、整段重抽与局段重绘入口，整段模式下隐藏单镜生成与单镜重拍入口并抑制重复发起（需求 1.5、3.6、4.1）
  - [x] 8.4 增加段时长越界与分镜不可参与整段生成的提示，并展示配置引导与重试入口（需求 2.6、2.8、3.7、3.8）
  - [x] 8.5 增加分段目标时长调整入口，调整后重算段边界并保留签名一致的段素材（需求 2.3、2.7）
  - [ ]* 8.6 扩展端到端测试，覆盖模式切换与持久化、段视图渲染、段素材落到段内分镜（需求 1.2、3.5）
  - [ ]* 8.7 扩展端到端测试，覆盖局段重绘替换段素材且段计划不变、切换模式后素材保留（需求 4.3、1.4）

- [x] 9. 时间轴、合成与导出
  - [x] 9.1 扩展 `src/drama/timeline.js` 的 `takes(project, opts)` 与段边界渲染，保留既有 `layout`、`shotAt` 与 `onSeek` 语义（需求 6.2、6.3）
  - [x] 9.2 扩展 `src/drama/compose.js` 的浏览器合成，按 `srcStart` seek 并按 `srcEnd - srcStart` 计数（需求 7.1）
  - [x] 9.3 扩展 `src/drama/compose.js` 的服务端提交，把段素材解引为 `videoUrl` 并写入 `srcStart`、`srcEnd`（需求 7.1，设计「时间轴与合成」）
  - [x] 9.4 扩展 `gate/server.py` 的 `drama_compose`，对带 `srcStart`、`srcEnd` 的镜头用 `-ss` 与 `-t` 从同一输入裁剪（需求 7.1）
  - [x] 9.5 扩展 `exportPack`，整段模式追加 `takes/take-<seq>.mp4` 与 `takes/takes.json`（需求 7.2）
  - [x] 9.6 在合成与导出前校验段素材缺失并提示待补齐的镜头段（需求 7.4、设计「错误处理」）
  - [ ]* 9.7 为 `timeline.takes` 与段素材切分编写单元测试（需求 6.1、6.2）
  - [ ]* 9.8 为 `gate/test_gate.py` 增加段内裁剪路径的测试（需求 7.1）

- [x] 10. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 11. 全量回归
  - [x] 11.1 运行前端单测、端到端测试与店门后端测试，确认既有基线保持通过（需求依赖与约束）
  - [ ]* 11.2 用即梦或方舟入口手工冒烟整段生成与局段重绘各一次，确认时间段提示与编辑意图的产出符合预期（设计「测试策略」）
